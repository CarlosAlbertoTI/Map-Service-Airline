import styled from 'styled-components'

export const MainContainer = styled.main`
    width:90vw;
    min-height:400px;
    margin: 0 auto;

    display: flex;
    flex-direction: column;
    margin-bottom:4rem ;

`